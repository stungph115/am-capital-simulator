"use client"
// app/city-comparison/error.tsx
import ErrorMessage from "@/components/ErrorMessage";

export default function Error() {
  return <ErrorMessage message="Erreur lors du chargement des données de comparaison." />;
}
